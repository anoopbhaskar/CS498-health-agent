# FitAgent

The production setup, API documentation, UI instructions, tests, and deployment notes now live in [`README.md`](README.md).

This lowercase file is retained for compatibility with links that referenced the original project readme.
